{
  "name": "starterbottypescript",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "publish": "bun run publish.ts",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && bun test"
  },
  "devDependencies": {
    "compressing": "^2.1.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
